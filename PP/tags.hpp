#pragma once

///
/// @brief Empty tag types. Used for overloading.
///
namespace PP::tags
{
constexpr inline struct d_t
{
} d;
constexpr inline struct o_t
{
} o;
constexpr inline struct p_t
{
} p;
constexpr inline struct r_t
{ } r; }
